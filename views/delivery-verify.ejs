<!DOCTYPE html>
<html>
<head>
  <title>Delivery Verification</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<div class="container py-5" style="max-width: 520px;">
  <h2 class="mb-4 text-center">Delivery Verification</h2>

  <% if (message) { %>
    <div class="alert alert-<%= color === 'green' ? 'success' : 'danger' %>"><%= message %></div>
  <% } %>

  <form method="POST" action="/delivery-verify">
      <div class="mb-3">
        <label class="form-label">Customer Mobile (or Email)</label>
        <input name="identifier" class="form-control" placeholder="e.g. 9876543210 OR user@email.com" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Delivery Code (6 digits)</label>
        <input name="code" class="form-control" maxlength="6" minlength="6" pattern="[0-9]{6}" required>
      </div>
      <button class="btn btn-primary w-100">Verify & Complete</button>
  </form>

</div>
</body>
</html>
